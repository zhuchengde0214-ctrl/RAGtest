# 合同 AI 审查与知识库检索 — Python 依赖

# PDF
pymupdf>=1.23.0

# LLM API
anthropic[bedrock]>=0.39.0   # 含 AWS Bedrock 后端依赖（boto3）
openai>=1.0.0                # 仅当使用 OpenAI Embedding 时需要

# 向量库 + 本地 embedding
chromadb>=0.5.0
sentence-transformers>=2.2.0

# 关键词检索
rank-bm25>=0.2.2
jieba>=0.42.1

# 工具
python-dotenv>=1.0.0
numpy>=1.24.0
